import { ReactElement } from 'react';

import { WrapperNumberPanel } from 'components/numberPanel/wrapperNumberPanel';

export const NumberPanel = (): ReactElement => (
  <WrapperNumberPanel>
    <span>0.022</span>
  </WrapperNumberPanel>
);
